rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Enable full read and write access for all collections
    // to allow client-driven state synchronization and automated seeding.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
